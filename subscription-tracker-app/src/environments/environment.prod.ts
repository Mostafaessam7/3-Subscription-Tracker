export const environment = {
  production: true,
  apiUrl: 'https://your-production-api.com/api',

  // Fill in the project DSN to enable Sentry. Empty means error reporting stays off — see
  // main.ts, which only initializes when this has a value.
  sentryDsn: ''
};
